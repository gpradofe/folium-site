---
const examples = [
  {
    title: "Generate an invoice",
    desc: "Pass structured data and get a pixel-perfect PDF back.",
    code: `curl -X POST https://api.folium.dev/v1/generate \\
  -H "Authorization: Bearer $FOLIUM_API_KEY" \\
  -H "Content-Type: application/json" \\
  -d '{
    "template": "invoice",
    "data": {
      "company": "Acme Corp",
      "items": [
        {"name": "Widget Pro", "qty": 5, "price": 29.99},
        {"name": "Gadget X", "qty": 2, "price": 49.99}
      ]
    }
  }' -o invoice.pdf`
  },
  {
    title: "Upload a custom template",
    desc: "Create reusable templates with your own layout and branding.",
    code: `curl -X POST https://api.folium.dev/v1/templates \\
  -H "Authorization: Bearer $FOLIUM_API_KEY" \\
  -H "Content-Type: application/json" \\
  -d '{
    "name": "my-report",
    "layout": {
      "page": {"size": "A4", "margin": 40},
      "header": {"text": "{{company}}"},
      "body": [
        {"type": "heading", "text": "{{title}}"},
        {"type": "table", "rows": "{{data.rows}}"}
      ]
    }
  }'`
  },
  {
    title: "Async with webhook callback",
    desc: "Fire-and-forget. Get notified when your PDF is ready.",
    code: `curl -X POST https://api.folium.dev/v1/generate \\
  -H "Authorization: Bearer $FOLIUM_API_KEY" \\
  -H "Content-Type: application/json" \\
  -d '{
    "template": "report",
    "data": {"title": "Q4 Report"},
    "webhook": "https://your-app.com/pdf-ready"
  }'

# {"job_id": "job_abc123", "status": "processing"}`
  }
];
---

<section id="api-examples" class="py-28 px-6">
  <div class="max-w-4xl mx-auto">
    <div class="text-center mb-16 animate-on-scroll">
      <div class="inline-flex items-center gap-2 bg-slate-800/50 border border-slate-700/50 rounded-full px-4 py-1.5 text-sm text-slate-400 mb-6">
        API examples
      </div>
      <h2 class="text-4xl md:text-5xl font-bold mb-5">See it in <span class="gradient-text">action.</span></h2>
      <p class="text-slate-400 text-lg">Real curl commands. Copy, paste, run.</p>
    </div>

    <div class="grid gap-6 stagger-children">
      {examples.map((ex) => (
        <div class="animate-on-scroll bg-slate-900/40 border border-slate-800/80 rounded-2xl overflow-hidden hover:border-emerald-500/20 transition-colors">
          <div class="px-6 pt-6 pb-3">
            <h3 class="text-lg font-semibold mb-1">{ex.title}</h3>
            <p class="text-slate-400 text-sm">{ex.desc}</p>
          </div>
          <pre class="px-6 pb-6 text-sm overflow-x-auto"><code class="text-emerald-300/80 font-mono">{ex.code}</code></pre>
        </div>
      ))}
    </div>
  </div>
</section>
